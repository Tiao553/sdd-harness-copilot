# dataviz

Placeholder domain created during the Copilot migration because `prompt-claude-to-copilot-v4.md` lists `dataviz` among the expected KB domains, but the source `.claude/kb/` tree did not contain a `dataviz` domain to copy.

No quick reference is included until source content exists.
